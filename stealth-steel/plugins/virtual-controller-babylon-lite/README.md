# Virtual Controller Babylon Lite Plugin

Provides the reusable DOM pointer controller and normalized joystick input used by the game.

Import the public API from `index.js`. Include `virtual-controller.css` after the host UI stylesheet so the host can provide safe-area custom properties.
